import { DataFrame, FieldConfig, TimeRange } from '@grafana/data';
import { GraphFieldConfig } from '@grafana/schema';
import { XYFieldMatchers } from './types';
export declare function preparePlotFrame(frames: DataFrame[], dimFields: XYFieldMatchers, timeRange?: TimeRange | null): DataFrame | null;
export declare function buildScaleKey(config: FieldConfig<GraphFieldConfig>): string;
