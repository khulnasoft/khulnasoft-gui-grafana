import { Story, Meta } from '@storybook/react';
import { Props } from './ClipboardButton';
declare const meta: Meta;
interface StoryProps extends Partial<Props> {
    inputText: string;
    buttonText: string;
}
export declare const ClipboardButton: Story<StoryProps>;
export declare const AsInputFieldAddon: Story<StoryProps>;
export default meta;
