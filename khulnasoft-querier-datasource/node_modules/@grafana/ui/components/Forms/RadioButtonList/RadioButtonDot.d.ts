import React from 'react';
export interface RadioButtonDotProps {
    id: string;
    name: string;
    checked?: boolean;
    disabled?: boolean;
    label: React.ReactNode;
    description?: string;
    onChange?: (id: string) => void;
}
export declare const RadioButtonDot: ({ id, name, label, checked, disabled, description, onChange }: RadioButtonDotProps) => JSX.Element;
