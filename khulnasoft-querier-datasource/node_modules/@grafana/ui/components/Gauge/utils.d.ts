import { DisplayValue, FieldConfig, GrafanaTheme, Threshold, ThresholdsConfig } from '@grafana/data';
interface GaugeAutoProps {
    titleFontSize: number;
    gaugeHeight: number;
    showLabel: boolean;
}
export declare const DEFAULT_THRESHOLDS: ThresholdsConfig;
export declare function calculateGaugeAutoProps(width: number, height: number, title: string | undefined): GaugeAutoProps;
export declare function getFormattedThresholds(decimals: number, field: FieldConfig, value: DisplayValue, theme: GrafanaTheme): Threshold[];
export {};
