import { TableCellProps } from './types';
interface CellActionProps extends TableCellProps {
    previewMode: 'text' | 'code';
}
export declare function CellActions({ field, cell, previewMode, onCellFilterAdded }: CellActionProps): JSX.Element;
export {};
