import 'lodash';
import 'marked';
import { escapeHtml, hasAnsiCodes, sanitize, sanitizeTextPanelContent, sanitizeUrl } from './sanitize.js';

const textUtil = {
  escapeHtml,
  hasAnsiCodes,
  sanitize,
  sanitizeTextPanelContent,
  sanitizeUrl
};

export { textUtil };
//# sourceMappingURL=index.js.map
